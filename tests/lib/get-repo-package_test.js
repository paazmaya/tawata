
import tape from 'tape';
import getRepoPackage from '../../lib/get-repo-package.js';

/*
tape('getRepoPackage - files does not exist', (test) => {
  test.plan(1);

  const input = {
    hoplaa: ''
  };

  const output = getRepoPackage(input);

  test.notOk(output);
});
*/
